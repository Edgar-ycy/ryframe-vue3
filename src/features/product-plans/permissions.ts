export const PRODUCT_PLAN_PERMISSIONS = Object.freeze({
  list: 'platform:product-plan:list',
  add: 'platform:product-plan:add',
  edit: 'platform:product-plan:edit',
  publish: 'platform:product-plan:publish',
})

export const TENANT_PRODUCT_PERMISSIONS = Object.freeze({
  view: 'tenant:product:view',
  assign: 'tenant:product:assign',
  override: 'tenant:capability:override',
})
